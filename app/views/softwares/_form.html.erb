	<%= simple_form_for(@software)  do |f| %>

			<%= f.input :softwarename, :collection => software_options, as: :check_boxes %>

				<div class="form-group">
      				<div class="col-lg-10 col-lg-offset-2">
  			<%= f.button :submit, 'Senden', class: 'btn btn-success'%>
					</div>
				</div>

	<% end %>